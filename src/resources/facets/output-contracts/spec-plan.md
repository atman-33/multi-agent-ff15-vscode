## Format

````markdown
---
change_name: mission-scoped-output-contracts
change_path: /home/atman/repos/multi-agent-ff15/openspec/changes/mission-scoped-output-contracts
---
````

## Rules

- `change_name` is required and must match the name of the created OpenSpec change directory
- `change_path` is recommended; if provided, it must match the workspace absolute path to the OpenSpec change directory
